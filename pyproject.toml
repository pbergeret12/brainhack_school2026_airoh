[project]
name = "airoh-mini"
version = "0.1.0"
requires-python = ">=3.11"
dependencies = [
    "airoh",
    "invoke",
    "requests",
    "jupyter>=1.0",
    "nbconvert>=7.0",
    "matplotlib>=3.5",
    "pandas>=1.3",
]

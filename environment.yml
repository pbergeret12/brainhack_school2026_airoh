name: project-env
channels:
  - conda-forge
dependencies:
  - python=3.11
  - requests
  - invoke
  - pip
  - pip:
      - airoh
      - jupyter>=1.0
      - nbconvert>=7.0
      - matplotlib>=3.5
      - pandas>=1.3
